# Account API

A small banking API with three endpoints: `POST /event`, `GET /balance` and `POST /reset`.

State is held in memory — durability is not a requirement of this challenge.

## Running

TODO
